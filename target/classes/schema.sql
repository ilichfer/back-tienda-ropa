-- ============================================================
-- OBSOLETO — este archivo ya NO se ejecuta.
--
-- El esquema de la base de datos ahora lo maneja Flyway con migraciones
-- versionadas en db/migration/ (empezando por V1__baseline.sql, que tiene
-- exactamente el contenido que tenía este schema.sql). Ver el comentario en
-- application.yml (bloque spring.flyway) para el porqué del cambio.
--
-- Este archivo se deja vacío (en vez de borrarlo) solo para que quede el
-- historial en git de qué reemplazó a qué. Es seguro borrarlo del todo
-- cuando quieras — spring.sql.init ya no apunta acá.
-- ============================================================
