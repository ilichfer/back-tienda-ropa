package com.tiendaropa.domain.repository;

import com.tiendaropa.domain.model.WaConversacion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WaConversacionRepository extends JpaRepository<WaConversacion, String> {
}
